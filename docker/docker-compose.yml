version: '3.7'

services:
  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_USER: humidity
      MYSQL_PASSWORD: password
      MYSQL_DATABASE: humidity
    ports:
    - 3306:3306